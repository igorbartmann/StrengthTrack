CREATE TABLE [dbo].[RESULTS]
(
  [ID]        INT NOT NULL  IDENTITY(1,1),
  [SESSIONID] INT NOT NULL,
  [VALUE]     INT NOT NULL,
  CONSTRAINT PK_RESULTS           PRIMARY KEY (ID),
  CONSTRAINT FK_SESSION_RESULTS   FOREIGN KEY (SESSIONID) REFERENCES [SESSIONS](ID)
);
GO;
